<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.swm.board.mapper.ListMapper">

    <select id="selectAll" resultType="kr.co.swm.board.list.model.dto.ListDto">
        select * from
    </select>

    <select id="getAllPosts" resultType="kr.co.swm.board.list.model.dto.ListDto">
        select count(*) from
    </select>

    <select id="getTotalCount" resultType="kr.co.swm.board.list.model.dto.ListDto">
        select count(*) from
    </select>

</mapper>
